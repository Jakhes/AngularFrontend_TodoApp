import { FilterPrioritiesPipe } from './filter-priorities.pipe';

describe('FilterPrioritiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrioritiesPipe();
    expect(pipe).toBeTruthy();
  });
});
